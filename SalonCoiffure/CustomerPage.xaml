<Page x:Class="SalonCoiffure.CustomerPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SalonCoiffure"
        mc:Ignorable="d"
        Title="CustomerPage" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Nom" />
            <TextBox Width="100" Margin="5" Text="{Binding SelectedCustomer.Nom, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Teléphone" />
            <TextBox Width="100" Margin="5" Text="{Binding SelectedCustomer.Telephone, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Email" />
            <TextBox Width="150" Margin="5" Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Adresse" />
            <TextBox Width="150" Margin="5" Text="{Binding SelectedCustomer.Adresse, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>


        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredCustomers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Adresse" Binding="{Binding Adresse}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Ajouter" Command="{Binding AddCommand}" Margin="5"/>
            <Button Content="Modifier" Command="{Binding UpdateCommand}" Margin="5"/>
            <Button Content="Supprimer" Command="{Binding DeleteCommand}" Margin="5"/>
            <Label Content="Rechercher par nom" />
            <TextBox Width="100" Margin="5"
         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
         />
            
        </StackPanel>
    </Grid>
</Page>
